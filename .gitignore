target/
*.class
*.log
logs/
.DS_Store
.idea/
*.iml
*.jar

# assignment PDFs kept local
*.pdf

# secrets & machine-local config — never committed
.env
/server.properties
/client.properties
# This installation's discovery id (E19.9). Machine-local by definition: committing
# it would give every clone of the repo the same identity, which is precisely the
# collision the id exists to prevent.
/server-id.properties
*.local.properties

# Submission documents carry personal ID numbers - never in the repo
docs/submission/
