# E6.10 / E6.11: the question editor, and a gate that was not one

**Branch:** `feat/e6-question-editor`, on top of `feat/e6-bank-screens` (PR #41)
**Epic:** E6, client half, second of two · **Contract:** `BANK_WIRE_CONTRACT.md`, **FROZEN v1**

---

## 0. What you need to do, in one place

Same header block as #41, for the second screen. It is a non-rail route reached from the list,
the pattern you named for `approvals.preview` and `grades.checked`.

| | |
|---|---|
| **Route id** | `questions.edit` |
| **Title** | (none needed; the screen writes its own heading per mode) |
| **Breadcrumb / rail** | **none**, two-arg `Route.shell` |
| **View class** | `client.features.bank.QuestionEditorView` |
| **Roles** | TEACHER, COORDINATOR. **Not PRINCIPAL** — see 3.6 |
| **Reached from** | the bank list's **Edit question** and **New question** buttons |
| **CSS beyond section 6** | none required |
| **pom** | one JaCoCo exclusion, `client/features/bank/QuestionEditorView*` |

**The id is spelled for the end state, not the interim one.** The list is `bank` until the
retirement PR swaps it behind `questions`; naming the editor `questions.edit` now means that PR
does not also rename this. Both ids live in `client.features.bank.BankRoutes`, which is the
feature's own copy of a spelling `client.core.Routes` owns, and `BankScreenWiringGuardTest` pins
the two against each other.

This PR is **not open yet.** It is complete apart from the two pieces your 2026-08-23 ruling ties
to the retirement, and it waits on #41's assembly. Section 8 says what is left.

---

## 1. What it does

`QUESTION_CREATE` and `QUESTION_UPDATE` behind a form: the stem, four answer boxes, a
`RadioGroup` naming which is correct, topic, difficulty, and an `ImagePicker`. E6.11's live rules
run while she types, and every server `VALIDATION` sentence lands under the box it names, which is
T-2.2's three-bad-saves case.

Three files on the house shape: `QuestionEditorCopy` (the editor's own words),
`QuestionEditorSession` (every decision, FX-free), `QuestionEditorView` (nodes only).

**`QuestionEditorCopy` deliberately contains none of the refusal sentences.** Every rule the
editor checks live is a rule the server also checks, and the server's wording is in
`BankMessages`. Restating one would give a teacher two accounts of one rule: told while typing
that two answers are too similar, then told something differently worded on save.

---

## 2. The design worth reviewing: the editor calls your validator instead of copying it

E6.11 has to reach **the same verdict as the server** while she types. Two ways to get that, and
only one survives contact with time: copy the rule and test that the copies agree, or call the
rule.

It calls it. `QuestionValidator.sameAnswer` is public for exactly this consumer and its javadoc
says so; the four length constants come from the same class, so the numbers the editor refuses at
cannot drift from the columns they stand in for.

**This is the decision in the PR I most want a yes or no on, and it is item 1 of section 5.**

The deployment risk was checked before the code was written, not after, and then independently by
the cold read: the client jar's `artifactSet` includes the whole project artifact, so server
classes ship in it, and the closure of these two imports is the JDK plus
`server.db.entities.Difficulty` and `server.db.projections.ReferencingExam`, both annotation-free.
Nothing pulls Hibernate, MySQL, Hikari, Flyway, PDFBox, POI or the Anthropic client. **There is no
`NoClassDefFoundError` waiting on defense day.**

There is a documentation trap behind it, though, and it is yours: see section 6.

---

## 3. The cold audit, which is again the substance

Run after the code existed, against the code. Seven confirmed findings and one suspected, all
fixed, each with a test, each test planted.

**3.1 The image gate did not exist, and this class's javadoc said it did.** ⚑

The headline, and it is about me. `forEdit` takes the illustration's bytes as a required
argument, and the javadoc claimed that made the bad state unreachable: *"There is no ordering for
a screen to get right, because there is no way to build the editor in the wrong order."*

Requiring the **parameter** is not requiring the **bytes**. Passing `null` for a question that has
a picture was accepted in silence, and my own test file constructed that state inside a nested
class titled "an editor cannot open without the illustration it is about".

`forEdit` now throws on it. The test asserts the refusal instead of narrating the harm.

**And the harm is not what either of us wrote down.** COMPONENTS-E6 §4.2 and my javadoc both said
a blind save "writes KEEP-of-nothing over her own picture". `QuestionService.imageFor` is
`case KEEP -> previous.getImage()`, so KEEP copies the blob forward and **the picture is not
lost.** The real damage is that the editor says "No illustration" about a question that has one,
and that a **Remove she presses is silently discarded** — she asks for the picture to go, the
server is told to keep it, and nothing anywhere says no.

**3.2 A save settling after she left the editor acted on wherever she was.** No liveness guard, and
the stale-edit path opens a modal and then navigates. A `CONFLICT` arriving while she was on the
grading queue would have thrown a dialog over it and dumped her on the bank list, about a question
she had stopped looking at. Now guarded by `navigator().isCurrent`.

**3.3 `baseVersionNo` never moved after a save.** `adopt` re-baselined every field except the
staleness token, so a second save from the same editor would cite the version before her own
previous save, get `CONFLICT`, and be told **"somebody else edited this question"** about herself.
Latent only because the screen navigates away on success, which is one feature request from being
live.

**3.4 A comment claimed a duplicate refusal marked both boxes.** It returns one, and the shape
only allows one. Worse, it named the earlier position while `QuestionValidator.answersDistinct`
reports `answerField(j)`, the later one. Now matches the server, and the comment says what the
code does. The **live** rule still marks both, which is where "both" was ever true.

**3.5 Two of your javadocs promise something the wire does not carry.** `Violation.field` exists so
"E6.11's editor can highlight the right box **without matching on message text**", and
`BankMessages` says the client "never has to match on text". `BankHandlers.fieldProblem` keeps only
the message, so the field has never reached the wire and E6.11 **does** match on text — by exact
equality against the catalogue, which is sound because both tiers ship in one artifact. Both
javadocs corrected in this PR. Whether to put the field on the wire is section 6.

**3.6 The principal must never be offered this screen.** F9.3 gives her zero mutating verbs and she
is on the bank's four reads only, so an editor would be a screen whose every Save is refused. That
is now a guard rather than a note, and it is the one row of contract section 2's table a client can
get wrong on its own.

**3.7 Three of my own assertions could not fail.** In the nested class named for the gate, three of
four tests survived removing the gate entirely. Replaced.

---

## 4. What the tests catch, planted rather than assumed

| Mutation | Caught by |
|---|---|
| `sameAnswer` replaced by `equals` | 10 tests, the whole contract §5 table |
| the TOPIC branch remapped to the stem's box | `Mapping.topic` |
| an unmatched sentence blanked instead of surfaced | `Mapping.unknownSentence` |
| the image gate removed | `theGateIsAGateAndNotAParameter` |
| `baseVersionNo` left unbaselined | `baseVersionFollowsTheSave` |
| `canEdit` back to asking the state enum | `editIsClosedOnAnEmptyBlob` |

**Three things the tests caught in me, worth recording because they are the same class of error.**
A hand-written seven-byte "PNG" the picker correctly refused, so a fixture proved nothing. A helper
that collected hidden labels, making a "does not contain" assertion unfalsifiable. And a TestFX
click that **silently pressed the image picker's Remove button** instead of the answer box it aimed
at, because the form scrolls and the robot clicks raw coordinates — found by a diagnostic run, not
by reading.

Writing the `canEdit` mutation also found a defect in my own fix: `QuestionImage` normalises a null
blob to an **empty array**, so a non-null check would have enabled the button and thrown on press.

---

## 5. What was volunteered, and what reversing it costs

Required by the task: the editor, its live rules, and the mapping from a refusal to a box. The
rest is mine, indexed so you can answer straight down it. **Item 1 is the only one I would call
expensive.**

| # | Decision | Completes or extends | Cost to reverse |
|---|---|---|---|
| **1** | **The editor imports `QuestionValidator` and `BankMessages` across the tier** | completes: E6.11 must reach the server's verdict | **~1 hour.** Duplicate the fold and the four constants, add an agreement test. It reintroduces two copies of one rule, which is the hazard this contract has closed twice |
| 2 | `BankRoutes` holds the feature's copy of both route ids | extends | one file; the alternative is literals at five call sites |
| 3 | Editor id spelled `questions.edit`, not `bank.edit` | extends | one constant here, one line in your assembly |
| 4 | The editor takes the detail and the bytes through `NavParams` rather than refetching by id | completes: it is what closes 3.1 at the screen level | moderate. Refetching needs its own loading state and reopens the ordering trap |
| 5 | `forEdit` throws rather than degrading | completes 3.1 | one line, and the gate stops being a gate |
| 6 | Two of your javadocs corrected (3.5) | extends | two comments |
| 7 | Four answer boxes plus a separate radio group, rather than interleaved rows | extends | a layout rewrite; interleaving means taking `RadioGroup` apart, and `ToggleGroup` is what guarantees C-8's at-most-one |
| 8 | **New question** uses the course filter and refuses to guess when nothing is filtered | extends | small; the alternative is a course picker inside the editor |

**One seam disclosed rather than left to be found.** `QuestionEditorSession.imageLogic()` is public
so the view can build its `ImagePicker` over the session's own logic instance. That is partly so
the FX-free tests can drive the picture path, and it has an independent justification: two objects
sharing one state is what stops "what is happening to the picture" existing in two places. The
alternative is the session mirroring the picker's state, which is the drift this avoids.

---

## 6. Two things for you

**6.1 `ARCHITECTURE.md` describes a client jar we do not build.** Line 251 says it *"excludes
DB/Hibernate/bot/server code via shade filters"*. The pom has never done that: the client
`artifactSet` includes the whole project artifact. Nothing depended on the difference until now,
and E6.11 is the first code whose correctness rests on the pom rather than on that sentence. An E20
packaging pass that "restores" the documented behaviour breaks the editor on open, and **no test
can see it**, because tests run on the full classpath. One clause in your file.

**6.2 Should `Violation.field` go on the wire?** Section 3.5. Today the client maps by exact
equality against `BankMessages`, which works and is tested, but it means **no sentence in that
class may be reworded without checking `QuestionEditorSession.locate`**. Putting the field on the
error payload would remove that coupling. The contract is FROZEN v1, so it is an amendment and
therefore yours. I have not done it; the javadocs now say what is true either way.

---

## 7. Build

`./mvnw -B clean verify` on JDK 21 with `HSTS_REQUIRE_MYSQL=true`, on `d8e58e7` over `4260f09`.

| | `main` at 4260f09 | this branch |
|---|---|---|
| Tests | 5131 measured at 8117d4c | **5507** |
| Failures | 0 | **2**, both `BankScreenWiringGuardTest` |
| Coverage, `QuestionEditorView*` excluded | 98.15% | **98.21%** |

**The two failures are the whole of what is missing**, and they name it: `questions.edit` is
registered for nobody, so `theEditorIsOfferedToAuthorsOnly` is red for TEACHER and COORDINATOR.
The other four guard cases went green the moment your assembly registered `bank`, and the two
negative cases stopped being vacuous at the same time: a real route now exists for them to assert
the principal and the student are not offered.

A red suite means JaCoCo never runs, so coverage was taken from a second pass with
`-Dmaven.test.failure.ignore=true`: same tests, same code, report phase allowed to finish.

`BankView` no longer needs excluding by me, since #41's assembly added it. **`QuestionEditorView`
still does**, and that line is in section 0's table alongside the route.

The 5131 baseline was measured at `8117d4c` before this work started; the branch now sits on
`4260f09`. The commits between are yours and Amjad's and were CI-green.

`QuestionEditorView` went from 1036 missed instructions and **zero covered** to 236. Nothing in the
build had ever constructed it, which is the same defect class as #25 one layer up: the screen
would have shipped and the first person to press Edit would have found out.

---

## 8. What is not in here

The two pieces your ruling ties together, both waiting on #41's assembly:

- **E6.14, the edit lock**, taken and released under `displayId5`, including the crash path the
  sweeper covers.
- **The legacy retirement**: the screen swap behind `questions`, `QuestionsView` and
  `LegacyQuestionHandlers` deleted, and `BankWiringGuardTest`'s allow-list shrinking by one entry,
  which is the proof you asked for. `Verb.java`'s two removals and the two legacy DTOs are yours.

E6.10 and E6.11 stay **unticked** in TODO.md until the screens are reachable, for the same reason
E6.9 does: a box a reviewer could disprove by opening the app.

---

## 9. Definition of Done

- [x] Cold auditor run AFTER the code was written, findings acted on — seven confirmed, all fixed
- [x] Matches the PRD ids named in the task (F2.1, F2.2, C-8, T-2.2)
- [x] Unit tests; coverage against a measured `main`, not a remembered one — 98.20% vs 98.15%
- [ ] Migrations — not applicable, no schema in this PR
- [x] No secrets, no dummy-credential changes in resources
- [x] TODO.md boxes ticked only where a reviewer could not disprove them (none, section 8)
- [ ] CI green — **not yet run; the PR is not open, and it will be red until the assembly lands**
