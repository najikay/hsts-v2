# Scenario 8 walked — three findings, two fixed

**Branch:** `fix/b-e12-screen-defects` · **Author:** Member B · **Reviewer:** Naji · **Date:** 2026-08-23

Walked acceptance scenario 8 end to end against a live server. Six of seven cases pass, one is
partial, and the walk found three things — **two of them in my own screens**, both in code the
coverage gate deliberately excludes.

## Verification

| Check | Result |
|---|---|
| `./mvnw verify` | see the CI run on this PR |
| Scenario 8 | 6 passed, 1 partial |
| New bugs | B-3 (open), B-4 (fixed), B-5 (fixed) |

## The grading story, end to end

This is the first time T-8 has been walked, and the pipeline holds:

- **8.1** — the queue opened with the Java sitting waiting, "8 sat · 8 marked · 8 still to
  approve". Every attempt already carried a score, computed **on submission** rather than by
  anything a teacher did.
- **8.2** — already evidenced by 9.1: `maya.levi` sat both exams and saw one row.
- **8.3** — the override refused a blank justification, with the sentence rather than a silent
  no-op.
- **8.4** — with a reason, the score moved, the row showed **Adjusted**, and the **Auto column
  kept the machine's original**. That is F8.3's audit trail: the change visible, the evidence
  intact.
- **8.5** — one verb served both single and bulk; the queue then emptied to "Nothing to grade";
  `maya.levi`'s notification bell showed **1** on her next sign-in (F8.4).
- **8.6** — her list went from one row to **two**. The grade that was invisible in 8.2 appeared
  the moment a teacher approved it. **That is C-3 end to end, in one screen** — the single best
  thing to demo from this epic.
- **8.7** — no class statistics anywhere, and structurally so.

## B-4 — Select-all selected nothing visible *(fixed here)*

Select-all updated the **session's** selection and not the **table's**. The button enabled and
the confirmation counted correctly, so the right rows were approved — but a teacher could not
*see* what she was about to approve, on the one action that cannot be undone.

The screen wired selection **table → session** and never **session → table**.

`render()` now drives the table's selection from the session, inside the same re-entrancy guard
the queue rail already uses, and **returns early when the two agree**. That early return is not
an optimisation: a render happens on every session change *including the one a teacher's click
caused*, so reselecting unconditionally would fight her mid-click.

**Why no test caught it.** The session tests pass because the session is genuinely correct —
`selectAllApprovable()` ticks exactly the right rows. The defect is in the view, which is
coverage-excluded by design. This is the class of defect a manual pass exists to catch, and it
is the argument for tomorrow's pass in one example.

## B-5 — My Grades truncated the date *(fixed here)*

The Approved column read **"23 Au…"**. The table divided its width evenly, so a date got the same
room as a two-character course code — and the column a student reads to know when her grade
arrived could not show a date. The "Teacher's note" header was clipped for the same reason.

Preferred widths per column, sized to content, still adaptive to the window.

Found by **looking at the rendered screen**. `MyGradesCopyTest` asserts the date formats to
"20 Aug 2026" and passes — it never sees a column. That is the honest limit of testing copy in
isolation, and worth saying out loud rather than treating the green tests as coverage of the
screen.

## ⚑ B-3 — `teacherComment` has no wire *(reported, not fixed — your call)*

**Read everywhere, written nowhere.** It is on the student wire, the teacher wire, both copy
classes and both screens. The only thing that ever sets it is the seed loader: no request DTO
carries a comment field, and no service calls `Grade.setTeacherComment`.

So **S-22 has no path through the application** — a teacher cannot leave a student the one piece
of free text the student is allowed to read. Acceptance case 8.4 asks for exactly this and cannot
pass.

**The seed masks it.** `yael.azulay` has a seeded comment, so every screen renders one and looks
finished. It is only visible if you try to *write* one.

Not to be confused with `overrideReason`, which is required, works, and is deliberately never
shown to the student. They are different fields for different audiences, and only one of them is
reachable.

| Option | Argument |
|---|---|
| **A** — add `teacherComment` to `GradeOverrideRequest` | Additive; reuses the gate and the dialog that already exist. But ties commenting to *changing a score*, and a teacher may want to comment on a grade she agrees with |
| **B** — a separate `GRADE_COMMENT_SET` verb | Matches how the two fields actually differ. But a new verb on a frozen contract, three days out |

**I would take A for v1** and note B as the v2 shape. It is a contract change either way, so it is
yours rather than mine.

## The fixture is spent

Walking 8.5 approved the Java sitting, so the "8 waiting to be graded" demo is gone until a
reseed. Reseeding restores it and re-resolves the relative execution windows, which the demo
needs anyway.

## Definition of Done

- [x] Scenario 8 recorded case by case, with the method and the evidence
- [x] B-4 and B-5 fixed, each with the reason in the code rather than only in the register
- [x] B-3 reported with two options and a recommendation
- [ ] Design-system components / screen review — **yours**; no new components, only column widths
- [x] Bugs register updated with case ids
- [ ] CI green — after push

## Next

9.3, the print view — the last unwalked case in scenario 9, and one that needs eyes on a rendered
page rather than a probe.
