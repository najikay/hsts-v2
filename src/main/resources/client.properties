# Default client connection settings (bundled fallback).
# For a remote server demo, copy this file next to hsts-client.jar
# and set server.host to the server machine's LAN IP address.

server.host=localhost
server.port=5555
